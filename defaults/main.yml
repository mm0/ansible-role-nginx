---
# defaults file for nginx
web_user: www-data
naked_domain_keys:
  private: /etc/privkey.pem;
  fullchain: /etc/fullchain.pem;
www_domain_keys:
  private: /etc/privkey.pem;
  fullchain: /etc/fullchain.pem;